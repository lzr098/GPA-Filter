# Git LFS tracking for reference BED files
# Uncomment after installing git-lfs (https://git-lfs.github.com/)
# Then run: git lfs install && git lfs track "refs/**/*.bed" && git lfs track "refs/**/*.bed.gz"
# refs/**/*.bed filter=lfs diff=lfs merge=lfs -text
# refs/**/*.bed.gz filter=lfs diff=lfs merge=lfs -text
